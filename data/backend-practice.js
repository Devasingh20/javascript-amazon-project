// const xhr = new XMLHttpRequest();
// xhr.open('GET', 'https://supersimplebackend.dev/products', true);// after the domain name the URL path is tells the server which resource you want to access.

// xhr.onreadystatechange = function () { // This function will be called whenever the readyState changes
//     if (xhr.readyState === 4 && xhr.status === 200) {
//         console.log(JSON.parse(xhr.responseText)); // This will log the response from the server
//     }
// }

// //for the response we can also use event listener.
// // xhr.addEventListener('load',()=>{
// //     console.log(xhr.responseText); // 
// // })


// xhr.send();



// const xhr = new XMLHttpRequest();
// xhr.open('GET', 'https://supersimplebackend.dev/hello', true);

// xhr.onreadystatechange = function () { // This function will be called whenever the readyState changes
//     if (xhr.readyState === 4 && xhr.status === 200) {
//         console.log((xhr.responseText)); // This will log the response from the server
//     }
// }
// xhr.send();